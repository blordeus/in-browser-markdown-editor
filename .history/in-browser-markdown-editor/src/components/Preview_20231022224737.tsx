import * as alias from 'module'
interface PreviewProps {
    markdown: string;
}
export const Preview = ({markdown}: PreviewProps) => {
    useEffect(() => {
    
    }, [input])
  return (
    <div>Preview</div>
  )
}
